{
  "private": true,
  "scripts": {
    "postinstall": "spago install",
    "clean": "rm -rf output .spago dist/*",
    "build": "spago build",
    "test": "spago test"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "parcel-bundler": "^1.12.4",
    "purescript": "^0.13.6",
    "spago": "^0.14.0"
  }
}
